{% extends 'base.html.twig' %}


{% block body %}

    <div class="inner-banner-w3ls py-5" id="home">

        <div class="container py-xl-5 py-lg-3">
            <!-- register  -->
            <div class="modal-body mt-md-2 mt-5">
                <h3 class="title-w3 mb-5 text-center text-wh font-weight-bold">Update Now</h3>
                <table bgcolor="#deb887" , border="3" , align="center" ,  style="width:80%"  >
                    <tr>
                {{form_start(f,{'attr':{'class':'m-form m-form--fit m-form--label-align-right','novalidate' : 'novalidate'}})}}
                <div class="m-portlet__body">


                    {{ form_label(f.image, 'image', {'label_attr': {'class': 'col-2 col-form-label'}}) }}
                    <div class="col-10" {{ form_errors(f.image) }}>
                        {{ form_widget(f.image, {'attr': {'class': 'form-control m-input'}}) }}

                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(f.nomenfant, 'Nom', {'label_attr': {'class': 'col-2 col-form-label'}}) }}
                    <div class="col-10">
                        {{ form_widget(f.nomenfant, {'attr': {'class': 'form-control m-input'}},{'attr':{'novalidate' : 'novalidate'}}) }}
                        {{ form_errors(f.nomenfant) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(f.prenomenfant, 'Prenom', {'label_attr': {'class': 'col-2 col-form-label'}}) }}
                    <div class="col-10">
                        {{ form_widget(f.prenomenfant, {'attr': {'class': 'form-control m-input'}},{'attr':{'novalidate' : 'novalidate'}}) }}
                        {{ form_errors(f.prenomenfant) }}
                    </div>
                </div>

                <div class="form-group">
                    {{ form_label(f.age , 'Age', {'label_attr': {'class': 'col-2 col-form-label'}}) }}
                    <div class="col-10">
                        {{ form_widget(f.age, {'attr': {'class': 'form-control m-input'}},{'attr':{'novalidate' : 'novalidate'}}) }}
                        {{ form_errors(f.age) }}
                    </div>
                </div>

                <div class="form-group">
                    {{ form_label(f.password ,'Password', {'label_attr': {'class': 'col-2 col-form-label'}}) }}
                    <div class="col-10">
                        {{ form_widget(f.password, {'attr': {'class': 'form-control m-input'}},{'attr':{'novalidate' : 'novalidate'}}) }}
                        {{ form_errors(f.password) }}
                    </div>
                </div>
                        <div class="form-group m-form__group row">
                            <div>
                                <style type="text/css">
                                    .btn_taille_egale{
                                        width:300px;
                                        height:40px;

                                    }
                                </style>
                                <p style="text-align: center;"><button type="submit" class="btn_taille_egale" style="background-color: #ace3cc"  >
                                        Modifier            </button></p>

                            </div>
                        </div>




                {{ form_end(f) }}
                    </tr>
                </table>
            </div>
            <!-- //register -->
        </div>
    </div>
{% endblock %}
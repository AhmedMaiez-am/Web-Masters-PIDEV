<form method="post" action="{{ path('contes_delete', {'idconte': conte.idconte}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conte.idconte) }}">
    <button class="btn">Delete</button>
</form>
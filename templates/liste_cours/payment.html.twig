{% extends 'base.html.twig' %}



{% block body %}
    <body>
    <header>
        <div class="container">
            <div class="header d-lg-flex justify-content-between align-items-center py-2 px-sm-2 px-1">

                <div id="logo">
                    <h1><a href="#listCours">KIDZY</a></h1>
                </div>
                <div class="nav_w3ls ml-lg-5">
                    <nav>
                        <label for="drop" class="toggle">Menu</label>
                        <input type="checkbox" id="drop" />
                        <ul class="menu">
                            <li><a href="{{ path ('redirection_cours')}}">Liste des cours</a></li>
                            <li><a href="{{ path ('redirection_contes')}}">Liste des contes</a></li>
                            <li><a href="{{ path ('redirection_inv_cours')}}">Inventaire cours</a></li>
                            <li><a href="{{ path ('redirection_inv_contes')}}">Inventaire contes</a></li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </header>

    <div class="main-w3pvt mian-content-wthree" id="pay">
        <div class="container">
            <div class="style-banner mx-auto">
                <h3 class="text-wh font-weight-bold text-center"><span>Paiement des cours</span></h3>
                <p class="mt-3 text-li text-center" id="join">Veuillez insérer votre nom de procéder au paiement du cours !</p>

                <form method="post" action="{{ path ('payer') }}">
                <input id="nom" class="form-control" type="text" name="nom" placeholder="Nom du parent..." aria-label="Nom">
                <input type="submit" class="form-control btn" value="Confirmer">
                </form>




                {% if lstP is defined %}
                {% for p in lstP %}
                {% endfor %}
                {% endif %}

            </div>
        </div>
    </div>

    <footer class="py-5">
        <div class="container py-xl-4 py-lg-3">
            <div class="row footer-grids">
                <div class="col-lg-2 col-6 footer-grid">
                    <h3 class="mb-sm-4 mb-3">Navigation</h3>
                    <ul class="list-unstyled">
                        <li>
                            <a href="{{ path ('redirection_cours')}}">Liste des cours</a>
                        </li>
                        <li>
                            <a href="{{ path ('redirection_contes')}}">Liste des contes</a>
                        </li>
                        <li>
                            <a href="{{ path ('redirection_inv_cours')}}">Inventaire cours</a>
                        </li>
                        <li>
                            <a href="{{ path ('redirection_inv_contes')}}">Inventaire contes</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>
    <a href="#pay" class="move-top text-center">
        <span class="fa fa-angle-double-up" aria-hidden="true"></span>
    </a>

    </body>
{% endblock %}
